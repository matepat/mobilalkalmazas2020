import { StyleSheet } from 'react-native';

const LabelStyles = StyleSheet.create({
    textLabel: {
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 10,
        color: '#595856'
    }
});

export default LabelStyles;